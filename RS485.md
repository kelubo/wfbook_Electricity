# RS-485

[TOC]

## 概述

**RS-485**（又称 TIA/EIA-485）是一种差分串行通信接口标准，由电信工业协会 (TIA) 和电子工业联盟 (EIA) 联合制定。它是工业现场总线中应用最广泛的物理层标准之一。

RS-485 采用 **平衡差分信号** 传输，使用一对双绞线（A、B 两根线），抗共模干扰能力强，支持多点通信（同一总线上最多可挂接 32 个标准负载节点，使用特制芯片可扩展至 256 个节点）。

---

## 电气特性

| 特性 | 参数 |
|------|------|
| 差分输出电压（带载） | ≥ ±1.5 V |
| 共模电压范围 | −7 V ~ +12 V |
| 接收器灵敏度 | ≥ ±200 mV |
| 接收器输入阻抗 | ≥ 12 kΩ |
| 最大节点数（标准） | 32 |
| 最大通信距离 | 约 1200 m（与速率成反比） |

### 信号定义

RS-485 使用差分信号线 A、B（部分文献标记为 D+、D− 或 Y、Z）：

- 逻辑 **1**：V(A) − V(B) ≥ +200 mV（A 线电压高于 B 线）
- 逻辑 **0**：V(A) − V(B) ≤ −200 mV（A 线电压低于 B 线）
- 在 −200 mV 到 +200 mV 之间为不确定状态

> **注意**：不同芯片厂商对 A/B 的极性定义可能相反，使用时需查阅数据手册。

---

## 传输模式

### 半双工（两线制）

最常用的 RS-485 组网方式。使用一对双绞线，同一时刻只能有一个节点发送数据，其余节点处于接收状态。需要进行 **方向控制**（收发切换）。

- 典型用于 Modbus RTU 协议

### 全双工（四线制）

使用两对双绞线，可同时发送和接收。主节点发送线与所有从节点接收线相连，主节点接收线与所有从节点发送线相连。

---

## 拓扑与终端匹配

### 总线拓扑

RS-485 要求严格的 **总线型（Daisy-chain）拓扑**，一条主线从头到尾串接所有节点，**不允许星型、树型等分支结构**。分支（stub）应尽量短（通常 ≤ 15 cm）。

### 终端电阻

为避免信号在总线两端反射，需在总线的首、尾两端各并接一个 **120 Ω 终端匹配电阻**（与线缆特性阻抗相同）。终端电阻应接在 A、B 线之间。

### 偏置电阻（失效保护）

当总线上所有节点均处于接收状态（总线空闲）时，A、B 之间的电压差可能落在不确定区域（< 200 mV），导致接收器输出不确定。此时可在总线上加 **偏置电阻**（上拉 A、下拉 B），保证空闲时 A−B > 200 mV，即逻辑 1 状态（空闲高电平）。

---

## 与 RS-232、RS-422 对比

| 特性 | RS-232 | RS-422 | RS-485 |
|------|--------|--------|--------|
| 信号方式 | 单端（非平衡） | 差分（平衡） | 差分（平衡） |
| 通信方式 | 全双工 | 全双工 | 半双工 / 全双工 |
| 最大节点数 | 1 发 1 收 | 1 发 10 收 | 32 节点（标准） |
| 最大距离 | ~15 m | ~1200 m | ~1200 m |
| 最大速率 | 115.2 kbps | 10 Mbps | 10 Mbps |
| 抗干扰能力 | 差 | 强 | 强 |
| 应用场景 | 电脑串口、调试 | 工业点对多点 | 工业多节点总线 |

---

## 速率与距离的关系

RS-485 的最高速率和最大距离相互制约：

| 速率 | 最大距离（约） |
|------|-------------|
| 100 kbps | 1200 m |
| 1 Mbps | 120 m |
| 10 Mbps | 12 m |

实际应用中还应考虑线缆质量、节点数量、电磁环境等因素，留有裕量。

---

## 常用协议

RS-485 仅定义了物理层（电气特性），不定义数据链路层及以上协议。常见运行在 RS-485 上的协议有：

- **Modbus RTU / ASCII**：工业领域最常用的串行通信协议
- **Profibus DP**：西门子主导的现场总线协议
- **BACnet MS/TP**：楼宇自动化协议
- **DMX512**：舞台灯光控制协议
- **自定义协议**：许多嵌入式设备使用私有协议

---

## 工程实践要点

1. **双绞线**：必须使用特性阻抗约 120 Ω 的屏蔽双绞线（如 Belden 9841）
2. **接地**：信号地（GND/COM）应在所有节点间连接，保证共模电压在允许范围内
3. **隔离**：工业现场建议使用隔离型 RS-485 芯片或外加光耦 / 磁隔离器件
4. **TVS 防护**：A、B 线对地加 TVS 管，防止浪涌和静电损坏
5. **终端电阻**：首尾两端各一个 120 Ω，中间节点不加
6. **上下拉电阻**：主机侧加偏置电阻（如 A 上拉 680 Ω 到 VCC，B 下拉 680 Ω 到 GND），保证总线空闲时处于确定状态
7. **线缆布线**：远离强电线缆（动力电缆），避免平行走线，必要时垂直交叉
